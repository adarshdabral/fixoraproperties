{
  "name": "fixora-properties",
  "private": true,
  "version": "0.1.0",
  "description": "Fixora Properties - direct property marketplace for buyers and sellers, with a platform-fee pricing model. See README.md: backend/ and frontend/ are independent projects, deployed separately (Render + Vercel).",
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "typecheck:backend": "npm run typecheck --prefix backend",
    "typecheck:frontend": "npm run typecheck --prefix frontend",
    "seed": "npm run seed --prefix backend"
  }
}
